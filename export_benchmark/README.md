# export_benchmark (deferred)

This folder is reserved for future model export, inference benchmarking, and deployment-facing scripts.

It is intentionally separated from `src/` so the core package remains focused on:

1. config
2. data generation
3. training
